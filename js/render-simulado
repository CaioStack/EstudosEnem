// Stub temporário: a implementação real dos simulados vem na v0.8.
function renderizarListaSimulados() {
  const painel = document.getElementById("painel-simulados");
  painel.innerHTML = `
    <div class="questao-card">
      <p>Os simulados serão implementados na <strong>v0.8</strong>,
      como provas reais do ENEM montadas automaticamente via API.</p>
    </div>
  `;
}